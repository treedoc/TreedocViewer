I’ll inspect the app structure first, then run it locally and reproduce the Chrome-only table lag with the CSV you named. I’ll check the exact file path too, since macOS usually uses ~/Downloads.


Listed files, ran 1 command
The CSV is present under /Users/jianwu.chen/Downloads/.... I’m going to look at the table rendering path and the Vite scripts so I can reproduce against the same code path before changing anything.


Explored 5 files, 1 search
The hot path renders every visible cell with several template function calls and Chrome-specific CSS selectors like td:has(...). I’m going to run the Vite app now and use Playwright’s Chromium to load the CSV, set page size to 500, and gather timing/trace data around scrolling.


Explored 4 files, 1 search, ran 3 commands
Chrome loaded the file and switched to 500 rows successfully. The test data has 18 columns, so the page is rendering about 9,000 cells plus hidden per-cell hover controls; I’m measuring scroll frame timing now and then I’ll isolate the CSS/DOM pieces that make Chrome worse.


Ran 2 commands
The timing run reproduced the Chrome lag clearly: baseline scrolling showed many long tasks and frame gaps over 2 seconds. Removing the per-cell hover controls and hover overflow brought scrolling back near normal, so I’m going to make the hover controls lazy-render only for the currently hovered cell and avoid unnecessary mouseover work while not dragging selection.


Explored 2 files
I’m editing TableView.vue now. The change keeps the same cell actions, but it stops mounting thousands of hidden hover button bars and skips cell hit-testing on ordinary mouseover events unless selection dragging is active.


Edited 1 file
The first code pass is in place. I’m re-running the Chrome scroll measurement against the live dev server to verify the DOM size drops and the frame gaps improve with the real CSV.

Ran node -e "const { chromium } = require('@playwright/test'); (async()=>{ const browser=await chromium.launch({channel:'chrome', headless:false}); const page=await browser.newPage({viewport:{width:1400,height:900}}); await page.goto('http://127.0.0.1:5173/'); await page.setInputFiles('input[type=file]', '/Users/jianwu.chen/Downloads/kube_persistentvolume_capacity_bytes-2026-05-28 18_44_41.csv'); await page.waitForSelector('.p-datatable-tbody tr'); await page.locator('.p-paginator .p-select').click(); await page.locator('[role=option], .p-select-option, .p-dropdown-item').filter({hasText:/^500$/}).click(); await page.waitForFunction(()=>document.querySelectorAll('.p-datatable-tbody tr').length===500, null, {timeout:10000}).catch(()=>{}); await page.waitForTimeout(1000); console.log('loaded', JSON.stringify({rows: await page.locator('.p-datatable-tbody tr').count(), cells: await page.locator('.cell-outer').count(), hoverBars: await page.locator('.cell-button-bar').count(), buttons: await page.locator('.cell-button-bar button').count()})); await page.evaluate(()=>{ window.__frames=[]; window.__long=[]; new PerformanceObserver(list=>{ window.__long.push(...list.getEntries().map(e=>e.duration)); }).observe({entryTypes:['longtask']}); let last=performance.now(); function raf(t){ window.__frames.push(t-last); last=t; if(window.__frames.length<500) requestAnimationFrame(raf); } requestAnimationFrame(raf); }); const c=page.locator('.p-datatable-table-container').first(); const box=await c.boundingBox(); await page.mouse.move(box.x+box.width/2, box.y+box.height/2); for(let i=0;i<80;i++){ await page.mouse.wheel(0,240); await page.waitForTimeout(16); } await page.waitForTimeout(600); console.log('fixed', JSON.stringify(await page.evaluate(()=>{ const frames=window.__frames||[]; const long=window.__long||[]; return {frames:frames.length, maxFrame:Math.max(...frames), over50:frames.filter(x=>x>50).length, over100:frames.filter(x=>x>100).length, avgFrame:frames.reduce((a,b)=>a+b,0)/frames.length, longTasks:long.length, maxLong:long.length?Math.max(...long):0, dom:document.querySelectorAll('.cell-outer').length, hoverBars:document.querySelectorAll('.cell-button-bar').length, buttons:document.querySelectorAll('.cell-button-bar button').length}; }))); await browser.close(); })().catch(e=>{console.error(e); process.exit(1);})"
The lazy hover controls cut the mounted buttons from 36,000 to effectively zero during scroll, and the worst long task dropped from about 2.3s to about 0.2s. There is still jank in Chrome, so I’m testing the remaining suspects: hover overflow and the :has() selection styles.

Ran node -e "const { chromium } = require('@playwright/test'); async function setup(page){ await page.goto('http://127.0.0.1:5173/'); await page.setInputFiles('input[type=file]', '/Users/jianwu.chen/Downloads/kube_persistentvolume_capacity_bytes-2026-05-28 18_44_41.csv'); await page.waitForSelector('.p-datatable-tbody tr'); await page.locator('.p-paginator .p-select').click(); await page.locator('[role=option], .p-select-option, .p-dropdown-item').filter({hasText:/^500$/}).click(); await page.waitForFunction(()=>document.querySelectorAll('.p-datatable-tbody tr').length===500, null, {timeout:10000}).catch(()=>{}); await page.waitForTimeout(1000);} async function measure(page,label){ await page.evaluate(()=>{ window.__frames=[]; window.__long=[]; new PerformanceObserver(list=>{ window.__long.push(...list.getEntries().map(e=>e.duration)); }).observe({entryTypes:['longtask']}); let last=performance.now(); function raf(t){ window.__frames.push(t-last); last=t; if(window.__frames.length<500) requestAnimationFrame(raf); } requestAnimationFrame(raf);}); const c=page.locator('.p-datatable-table-container').first(); const box=await c.boundingBox(); await page.mouse.move(box.x+box.width/2, box.y+box.height/2); for(let i=0;i<80;i++){ await page.mouse.wheel(0,240); await page.waitForTimeout(16);} await page.waitForTimeout(600); console.log(label, JSON.stringify(await page.evaluate(()=>{ const f=window.__frames||[], l=window.__long||[]; return {maxFrame:Math.max(...f), over50:f.filter(x=>x>50).length, over100:f.filter(x=>x>100).length, avg:f.reduce((a,b)=>a+b,0)/f.length, longTasks:l.length, maxLong:l.length?Math.max(...l):0, hoverBars:document.querySelectorAll('.cell-button-bar').length}; }))); } (async()=>{ const browser=await chromium.launch({channel:'chrome', headless:false}); const page=await browser.newPage({viewport:{width:1400,height:900}}); await setup(page); await measure(page,'lazy'); await page.addStyleTag({content:'.cell-outer:hover .cell-wrapper{overflow:hidden!important}'}); await measure(page,'no-hover-overflow'); await page.evaluate(()=>{ for(const sheet of [...document.styleSheets]){ let rules; try{rules=sheet.cssRules}catch(e){continue} for(let i=rules.length-1;i>=0;i--){ if(rules[i].cssText.includes(':has(')) sheet.deleteRule(i); } } }); await measure(page,'no-hover-overflow-no-has'); await browser.close(); })().catch(e=>{console.error(e); process.exit(1);})"
The remaining Chrome cost is the td:has(...) selection CSS. Deleting those rules at runtime reduced the worst frame gap to about 83ms and nearly eliminated >50ms frames. I’m replacing those selectors with equivalent classes on the existing .cell-outer, which avoids Chrome’s expensive ancestor matching across 9,000 cells.


Edited 1 file, explored 1 search, ran 1 command
The production build passes. I’m doing one final Chrome measurement with the :has() selectors removed from source so we have numbers for the actual fix.

Ran node -e "const { chromium } = require('@playwright/test'); (async()=>{ const browser=await chromium.launch({channel:'chrome', headless:false}); const page=await browser.newPage({viewport:{width:1400,height:900}}); await page.goto('http://127.0.0.1:5173/'); await page.setInputFiles('input[type=file]', '/Users/jianwu.chen/Downloads/kube_persistentvolume_capacity_bytes-2026-05-28 18_44_41.csv'); await page.waitForSelector('.p-datatable-tbody tr'); await page.locator('.p-paginator .p-select').click(); await page.locator('[role=option], .p-select-option, .p-dropdown-item').filter({hasText:/^500$/}).click(); await page.waitForFunction(()=>document.querySelectorAll('.p-datatable-tbody tr').length===500, null, {timeout:10000}).catch(()=>{}); await page.waitForTimeout(1000); console.log('loaded', JSON.stringify({rows: await page.locator('.p-datatable-tbody tr').count(), cells: await page.locator('.cell-outer').count(), hoverBars: await page.locator('.cell-button-bar').count(), buttons: await page.locator('.cell-button-bar button').count()})); await page.evaluate(()=>{ window.__frames=[]; window.__long=[]; new PerformanceObserver(list=>{ window.__long.push(...list.getEntries().map(e=>e.duration)); }).observe({entryTypes:['longtask']}); let last=performance.now(); function raf(t){ window.__frames.push(t-last); last=t; if(window.__frames.length<500) requestAnimationFrame(raf); } requestAnimationFrame(raf); }); const c=page.locator('.p-datatable-table-container').first(); const box=await c.boundingBox(); await page.mouse.move(box.x+box.width/2, box.y+box.height/2); for(let i=0;i<80;i++){ await page.mouse.wheel(0,240); await page.waitForTimeout(16); } await page.waitForTimeout(600); console.log('final', JSON.stringify(await page.evaluate(()=>{ const frames=window.__frames||[]; const long=window.__long||[]; return {frames:frames.length, maxFrame:Math.max(...frames), over50:frames.filter(x=>x>50).length, over100:frames.filter(x=>x>100).length, avgFrame:frames.reduce((a,b)=>a+b,0)/frames.length, longTasks:long.length, maxLong:long.length?Math.max(...long):0, dom:document.querySelectorAll('.cell-outer').length, hoverBars:document.querySelectorAll('.cell-button-bar').length, buttons:document.querySelectorAll('.cell-button-bar button').length}; }))); await browser.close(); })().catch(e=>{console.error(e); process.exit(1);})"
The source-level run still shows Chrome jank, which means the hover overflow rule is also contributing when Chrome scrolls under the pointer. I’m checking whether making cell overflow stable, instead of changing it on :hover, resolves that without removing the ability to scroll long cell contents.

Ran node -e "const { chromium } = require('@playwright/test'); async function setup(page){ await page.goto('http://127.0.0.1:5173/'); await page.setInputFiles('input[type=file]', '/Users/jianwu.chen/Downloads/kube_persistentvolume_capacity_bytes-2026-05-28 18_44_41.csv'); await page.waitForSelector('.p-datatable-tbody tr'); await page.locator('.p-paginator .p-select').click(); await page.locator('[role=option], .p-select-option, .p-dropdown-item').filter({hasText:/^500$/}).click(); await page.waitForFunction(()=>document.querySelectorAll('.p-datatable-tbody tr').length===500, null, {timeout:10000}).catch(()=>{}); await page.waitForTimeout(1000);} async function measure(page,label){ await page.evaluate(()=>{ window.__frames=[]; window.__long=[]; new PerformanceObserver(list=>{ window.__long.push(...list.getEntries().map(e=>e.duration)); }).observe({entryTypes:['longtask']}); let last=performance.now(); function raf(t){ window.__frames.push(t-last); last=t; if(window.__frames.length<500) requestAnimationFrame(raf); } requestAnimationFrame(raf);}); const c=page.locator('.p-datatable-table-container').first(); const box=await c.boundingBox(); await page.mouse.move(box.x+box.width/2, box.y+box.height/2); for(let i=0;i<80;i++){ await page.mouse.wheel(0,240); await page.waitForTimeout(16);} await page.waitForTimeout(600); console.log(label, JSON.stringify(await page.evaluate(()=>{ const f=window.__frames||[], l=window.__long||[]; return {maxFrame:Math.max(...f), over50:f.filter(x=>x>50).length, over100:f.filter(x=>x>100).length, avg:f.reduce((a,b)=>a+b,0)/f.length, longTasks:l.length, maxLong:l.length?Math.max(...l):0}; }))); } (async()=>{ const browser=await chromium.launch({channel:'chrome', headless:false}); const page=await browser.newPage({viewport:{width:1400,height:900}}); await setup(page); await measure(page,'current'); await page.addStyleTag({content:'.cell-outer:hover .cell-wrapper{overflow:hidden!important}.cell-wrapper{overflow:hidden!important}'}); await measure(page,'always-hidden'); await page.addStyleTag({content:'.cell-wrapper{overflow:auto!important;overflow:overlay!important}.cell-outer:hover .cell-wrapper{overflow:auto!important;overflow:overlay!important}'}); await measure(page,'always-auto'); await browser.close(); })().catch(e=>{console.error(e); process.exit(1);})"
Worked for 7m 26s
Stable overflow is the other fix: forcing cells to keep the same overflow mode during scroll reduced Chrome frame gaps sharply, while toggling overflow on hover kept causing style work. I’m making cell-wrapper use stable overflow and removing the hover overflow toggle.